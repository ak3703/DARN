int fib(int x)
{
  if (x < 2) return 1;
  return fib(x-1) + fib(x-2);
}


int main() {
	
	/* Instantiating variables */
	int i;
	bool b;
	int[3] a;
	a = [1,2,3];

	/* print 0-3 */
	for (i=0;i<3;i=i+1) {
		print(a[i]);
		prints("\n");
	}

	i = 0;
	b = false;
	while (i<2) {
		if (!b) {
		    prints("This is true\n");
		} else {
			prints("This is false\n");
		}
		b = true;
		i=i+1;
	}

	print(fib(4));
	prints("\n");

}

