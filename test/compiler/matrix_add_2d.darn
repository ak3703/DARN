int main() {
	
	int[4][5] x;
	int[4][5] y;

	int i;
	int j;
	for (i=0; i<height(x); i=i+1) {
		for (j=0; j<width(x); j=j+1) {
			x[i][j] = 1;
			y[i][j] = 1;
		}
	}

	add_2D_int(%%x, %%y, height(x), width(x));

	print(x[0]);

}
